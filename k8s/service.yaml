apiVersion: v1
kind: Service
metadata:
  name: ai-travel-planner-service
  labels:
    app: ai-travel-planner
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: ai-travel-planner
